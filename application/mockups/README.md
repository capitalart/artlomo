# Mockup backend (Phase 1)

Deterministic, index-driven mockup generation. No UI, no routes.

## Invariants

- All path resolution flows through JSON indexes.

  - `lab/index/artworks.json` → locate processed artwork folder and assets file.

  - `<slug>-assets.json` (in the artwork folder) → locate required files (`*-ANALYSE.jpg`, metadata, QC, thumb) and declare the mockups subfolder.

- Never scan directories or guess filenames. If a path is not declared in an index, it does not exist.

- Artwork input is always `<slug>-ANALYSE.jpg` from the assets index. The print master is never used here.

- Hash = `sha256` over raw bytes of: analyse image + base PNG + coordinate JSON.

- Outputs are stable JPEGs: quality 95, subsampling 0, optimize False. Thumbs use long-edge 600px with Lanczos.

## Slot semantics

- `mode="generate"`: slot must be empty; hash mismatch (if provided) is an error; non-destructive.

- `mode="swap"`: slot must exist; old composite and thumb are deleted before writing the new ones; hash mismatches are expected.

- Slot keys are zero-padded (`"01"`, `"02"`).

## Coordinate schema

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}
```

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text
{
  "template": "Bedroom-Adults-01",
  "regions": [
    {"id": "primary", "corners": [TL, TR, BL, BR]},
    ... up to 6 regions
  ]
}

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (17)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (17)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (18)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (18)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (19)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (19)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (17)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (17)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (18)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (18)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (19)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (19)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (20)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (20)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (21)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (21)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (22)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (22)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (23)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (23)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (24)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (24)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (20)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (20)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (25)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (25)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (17)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (17)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (21)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (21)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (26)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (26)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (17)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (17)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (17)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (17)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (22)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (22)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (18)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (18)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (19)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (19)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (27)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (27)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (18)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (18)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (18)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (18)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (20)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (20)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (21)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (21)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (23)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (23)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (28)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (28)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (24)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (24)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (29)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (29)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (17)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (17)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (18)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (18)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (30)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (30)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (19)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (19)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (19)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (19)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (19)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (19)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (25)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (25)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (17)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (17)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (31)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (31)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (26)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (26)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (18)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (18)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (27)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (27)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (19)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (19)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (32)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (32)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (33)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (33)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (20)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (20)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (20)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (20)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (34)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (34)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (21)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (21)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (21)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (21)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (28)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (28)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (22)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (22)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (23)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (23)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (20)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (20)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (24)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (24)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (29)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (29)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (35)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (35)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (22)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (22)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (22)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (22)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (25)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (25)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (21)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (21)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (22)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (22)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (12)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (17)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (17)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (36)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (36)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (23)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (23)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (23)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (23)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (15)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (37)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (37)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (24)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (24)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (24)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (24)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (30)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (30)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (9)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (26)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (26)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (18)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (18)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (27)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (27)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (19)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (19)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (23)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (23)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (31)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (31)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (20)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (20)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (38)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (38)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (13)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (20)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (20)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (2)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (39)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (39)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (25)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (25)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (25)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (25)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (4)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (28)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (28)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (17)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (17)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (3)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (10)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (11)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (24)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (24)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (5)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (25)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (25)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (6)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (7)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (16)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (8)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text

Points must lie within the base PNG bounds. Phase 1 uses all regions with the same artwork.

## Generated assets (14)

- Composite: `mockups/mu-<slug>-<slot>.jpg`

- Thumb: `mockups/thumbs/mu-<slug>-<slot>-THUMB.jpg`

- Slot entry (written atomically into `<slug>-assets.json`):

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}
```

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text
{
  "template_slug": "Bedroom-Adults-01",
  "aspect_ratio": "4x5",
  "regions": ["primary"],
  "composite": "mockups/mu-<slug>-01.jpg",
  "thumb": "mockups/thumbs/mu-<slug>-01-THUMB.jpg",
  "hash": "sha256:…",
  "updated_at": "ISO-8601"
}

```text

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.

## Usage

Call `generate_mockups_for_artwork()` in `application.mockups.pipeline` with a SKU, slot, mode, mockup base slug (stored as `template_slug` in the slot schema), aspect ratio, base PNG path, and coordinate JSON path. The function resolves artwork and assets via indexes, validates inputs, writes composites/thumbs atomically, and updates the assets index. Downstream systems must always consume paths from the updated `<slug>-assets.json`—never by convention.
